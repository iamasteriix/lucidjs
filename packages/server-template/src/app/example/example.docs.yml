/:
  get:
    summary: Module example so you know you're in the right place.
    tags:
      - Examples
    responses:
      '200':
        description: Welcome message
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: rocketship, rocketship
                time:
                  type: string
                  format: date-time